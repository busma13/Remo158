<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Steam Wishlist</title>
    <link rel="stylesheet" href="css/style.css">
</head>

<body>

    <% if (locals.messages.errors) { %>
        <% messages.errors.forEach( el=> { %>
            <div class="alert alert-danger">
                <%= el.msg %>
            </div>
            <% }) %>
                <% } %>
                    <div class="container-user">
                        <h1>Sign Up</h1>
                        <form action="/signup" method="POST">
                            <label> Username:</label>
                            <input class="form-label" type="text" name="userName" placeholder="User Name">
                            <label> Email:</label>
                            <input class="form-label" type="email" name="email" placeholder="Email">
                            <label> Password:</label>
                            <input class="form-label" type="password" name="password" placeholder="Password">
                            <label> Confirm Password:</label>
                            <input class="form-label" type="password" name="confirmPassword"
                                placeholder="Confirm Password">
                            <input class="form-label" type="submit" value="Sign Up">
                        </form>
                        <div>
                            Already have an account?
                            <a class="sign-up-in" href="/login">
                                    Sign in!
                            </a>
                        </div>
                    </div>
</body>

</html>